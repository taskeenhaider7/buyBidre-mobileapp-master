import React from 'react';
import {Button, Text, View} from 'react-native';

const Sidebar = ({navigation}) => {
    return (
        <View>
            <Text>Sidebar components</Text>
        </View>

    );
};

export default Sidebar;
