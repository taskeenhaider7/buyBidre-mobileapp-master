import React from 'react';
import {Button, Text, View} from 'react-native';

const Privacy = ({navigation}) => {
    return (
        <View>
            <Text>Privacy components</Text>
        </View>

    );
};

export default Privacy;
